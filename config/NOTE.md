# Thing to keep this directory around

I'm switching away from ninja-multiconfig, because it wasn't integrating well
with VS.

With C++ modules, the "mod_cfg.cmake" file needs to be generated by running "bun
run mod_cmake".
